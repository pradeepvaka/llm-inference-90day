# Labs

Labs live in the daily notebooks under `../notebooks/` — each day's packet
PDF (`../packets/dayNN.pdf`) walks through the lab steps with expected
outputs.

Use this directory for your own work: notes, result tables, benchmark
numbers from your runs, and anything you want to keep as a record of the
90 days. Commit early and often — the git history is the diary.
